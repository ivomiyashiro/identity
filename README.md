```powershell
dotnet user-secrets set "ConnectionStrings:DefaultConnection" ""

dotnet user-secrets set "Jwt:Secret" ""

dotnet user-secrets set "ApiKeys:Resend" ""

dotnet user-secrets set "Google:ClientId" ""
```